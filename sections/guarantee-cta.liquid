<section class="unikor-guarantee">
  <div class="page-width unikor-guarantee__grid">
    <div class="unikor-guarantee__media">
      {% if section.settings.video_image_url != blank %}
        <img src="{{ section.settings.video_image_url }}" alt="" width="500" height="320" loading="lazy">
      {% elsif section.settings.video_image != blank %}
        <img src="{{ section.settings.video_image | image_url: width: 500 }}" alt="" width="500" height="320" loading="lazy">
      {% else %}
        <div class="unikor-guarantee__placeholder"><span>Image / vidéo</span></div>
      {% endif %}
    </div>
    <div class="unikor-guarantee__content">
      <h2 class="unikor-guarantee__title">{{ section.settings.heading | default: 'En finir avec le pipi au lit ou remboursé' }}</h2>
      <div class="unikor-guarantee__text">
        {% if section.settings.body != blank %}{{ section.settings.body }}{% else %}<p>Testez le NightGuard™ Bedwetting Alarm pendant 60 jours. Si vous ne constatez pas d'amélioration nette pour votre enfant, contactez-nous. Nous vous remboursons intégralement, sans discussion !</p>{% endif %}
      </div>
      <p class="unikor-guarantee__risk">Essayez le NightGuard™ <strong>sans risque.</strong></p>
      <p class="unikor-guarantee__ready">Prêt à aider votre enfant à rester au sec ?</p>
      <p class="unikor-guarantee__get">Commandez le NightGuard Bedwetting Alarm dès aujourd'hui !</p>
      {% if section.settings.button_label != blank %}
        <a href="{{ section.settings.button_link | default: '/cart' }}" class="unikor-btn unikor-btn--black unikor-guarantee__btn">{{ section.settings.button_label }}</a>
      {% endif %}
    </div>
  </div>
</section>

{% schema %}
{
  "name": "Guarantee + CTA",
  "class": "section-guarantee-cta",
  "tag": "section",
  "settings": [
    { "type": "text", "id": "heading", "label": "Heading", "default": "En finir avec le pipi au lit ou remboursé" },
    { "type": "html", "id": "body", "label": "Body HTML" },
    { "type": "text", "id": "video_image_url", "label": "Video / image URL", "info": "Optional external image URL (overrides picker when set)." },
    { "type": "image_picker", "id": "video_image", "label": "Video / image" },
    { "type": "text", "id": "button_label", "label": "Button label", "default": "Commander" },
    { "type": "url", "id": "button_link", "label": "Button link" }
  ]
}
{% endschema %}
